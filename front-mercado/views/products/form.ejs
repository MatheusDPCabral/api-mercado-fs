<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <!-- Importando o Materialize CSS -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
</head>
<body>
    <div class="container">
        <h1 class="center-align"><%= title %></h1>
        <div class="row">
            <form class="col s12" id="productForm" action="<%= action %>" method="POST">
                <div class="row">
                    <div class="input-field col s12">
                        <input id="name" type="text" class="validate" name="name" value="<%= product ? product.name : '' %>" required>
                        <label for="name">Nome</label>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s6">
                        <input id="price" type="number" class="validate" name="price" value="<%= product ? product.price : '' %>" required>
                        <label for="price">Preço</label>
                    </div>
                    <div class="input-field col s6">
                        <input id="stock" type="number" class="validate" name="stock" value="<%= product ? product.stock : '' %>" required>
                        <label for="stock">Quantidade</label>
                    </div>
                </div>
                <div class="row">
                    <div class="col s12">
                        <button class="btn waves-effect waves-light" type="submit">Salvar</button>
                        <a class="btn waves-effect waves-light red" href="/products">Cancelar</a>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Importando o Materialize JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script src="/javascripts/validation.js"></script>
</body>
</html>
